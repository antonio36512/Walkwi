{
  "name": "walkwi-admin",
  "private": true,
  "scripts": {
    "dev": "concurrently --names API,WEB --prefix-colors green,cyan --restart-tries -1 --restart-after 1000 \"node --watch server/src/server.js\" \"npm --prefix client run dev\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm --prefix client run build",
    "start": "node server/src/server.js",
    "portal": "npm run build && node --watch server/src/server.js"
  },
  "devDependencies": { "concurrently": "^9.2.1" }
}
